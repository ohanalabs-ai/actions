name: SLSA report
description: Verify GitHub artifact attestations for a container image and generate a reusable markdown report.

inputs:
  subject:
    description: Full OCI subject in image@digest form
    required: true
  image-base:
    description: OCI image repository without digest
    required: false
    default: ""
  digest:
    description: OCI digest for display
    required: false
    default: ""
  signer-workflow:
    description: Full signer workflow path or repo-qualified workflow identity accepted by gh attestation verify
    required: true
  repo:
    description: Repository owning the subject artifact
    required: false
    default: ${{ github.repository }}
  gh-token:
    description: GitHub token used by gh for verification
    required: false
    default: ${{ github.token }}
  ref-name:
    description: Git ref associated with the build
    required: false
    default: ${{ github.ref_name }}
  title:
    description: Report title
    required: false
    default: SLSA verification
  fail-on-unverified:
    description: Whether to fail if verification fails
    required: false
    default: "true"

outputs:
  image:
    description: Verified image base
    value: ${{ steps.vars.outputs.image }}
  digest:
    description: Verified digest
    value: ${{ steps.vars.outputs.digest }}
  subject:
    description: Full image reference with digest
    value: ${{ steps.vars.outputs.subject }}
  verified:
    description: true when attestation verification succeeded
    value: ${{ steps.verify.outputs.verified }}
  signer_workflow:
    description: Signer workflow used during verification
    value: ${{ steps.vars.outputs.signer_workflow }}
  slsa_level:
    description: Claimed SLSA posture
    value: ${{ steps.vars.outputs.slsa_level }}
  summary_markdown:
    description: Markdown summary suitable for PR comments or step summaries
    value: ${{ steps.summary.outputs.markdown }}
  verification_command:
    description: CLI command users can execute to verify the attestation
    value: ${{ steps.vars.outputs.verification_command }}

runs:
  using: composite
  steps:
    - name: Resolve verification vars
      id: vars
      shell: bash
      env:
        SUBJECT: ${{ inputs.subject }}
        IMAGE_BASE: ${{ inputs.image-base }}
        DIGEST: ${{ inputs.digest }}
        SIGNER_WORKFLOW: ${{ inputs.signer-workflow }}
        REPO: ${{ inputs.repo }}
      run: |
        set -euo pipefail

        IMAGE="${IMAGE_BASE}"
        DIGEST_VALUE="${DIGEST}"

        if [ -z "${IMAGE}" ] || [ -z "${DIGEST_VALUE}" ]; then
          IMAGE="${SUBJECT%@*}"
          DIGEST_VALUE="${SUBJECT#*@}"
        fi

        VERIFY_SUBJECT="oci://${SUBJECT#oci://}"

        echo "image=${IMAGE}" >> "$GITHUB_OUTPUT"
        echo "digest=${DIGEST_VALUE}" >> "$GITHUB_OUTPUT"
        echo "subject=${SUBJECT}" >> "$GITHUB_OUTPUT"
        echo "verify_subject=${VERIFY_SUBJECT}" >> "$GITHUB_OUTPUT"
        echo "signer_workflow=${SIGNER_WORKFLOW}" >> "$GITHUB_OUTPUT"
        echo "slsa_level=3" >> "$GITHUB_OUTPUT"

        {
          echo "verification_command<<EOF"
          printf 'gh attestation verify %q --repo %q --signer-workflow %q\n' \
            "${VERIFY_SUBJECT}" \
            "${REPO}" \
            "${SIGNER_WORKFLOW}"
          echo "EOF"
        } >> "$GITHUB_OUTPUT"


    - name: Verify artifact attestation
      id: verify
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.gh-token }}
        SUBJECT: ${{ steps.vars.outputs.subject }}
        REPO: ${{ inputs.repo }}
        SIGNER_WORKFLOW: ${{ steps.vars.outputs.signer_workflow }}
        FAIL_ON_UNVERIFIED: ${{ inputs.fail-on-unverified }}
      run: |
        set +e

        VERIFY_SUBJECT="oci://${SUBJECT#oci://}"
        OUTPUT_FILE="$RUNNER_TEMP/attestation-verify.txt"
        gh attestation verify "${VERIFY_SUBJECT}" \
          --repo "${REPO}" \
          --signer-workflow "${SIGNER_WORKFLOW}" \
          >"${OUTPUT_FILE}" 2>&1
        STATUS=$?

        if [ "${STATUS}" -eq 0 ]; then
          echo "verified=true" >> "$GITHUB_OUTPUT"
        else
          echo "verified=false" >> "$GITHUB_OUTPUT"
        fi

        {
          echo "log<<EOF"
          cat "${OUTPUT_FILE}"
          echo "EOF"
        } >> "$GITHUB_OUTPUT"

        if [ "${STATUS}" -ne 0 ] && [ "${FAIL_ON_UNVERIFIED}" = "true" ]; then
          cat "${OUTPUT_FILE}"
          exit "${STATUS}"
        fi

        exit 0

    - name: Build markdown report
      id: summary
      shell: bash
      env:
        TITLE: ${{ inputs.title }}
        SUBJECT: ${{ steps.vars.outputs.subject }}
        IMAGE: ${{ steps.vars.outputs.image }}
        DIGEST: ${{ steps.vars.outputs.digest }}
        VERIFIED: ${{ steps.verify.outputs.verified }}
        VERIFY_LOG: ${{ steps.verify.outputs.log }}
        VERIFY_COMMAND: ${{ steps.vars.outputs.verification_command }}
        SIGNER_WORKFLOW: ${{ steps.vars.outputs.signer_workflow }}
        SLSA_LEVEL: ${{ steps.vars.outputs.slsa_level }}
        REF_NAME: ${{ inputs.ref-name }}
      run: |
        set -euo pipefail

        if [ "${VERIFIED}" = "true" ]; then
          STATUS_ICON="✅"
          STATUS_TEXT="Verified"
        else
          STATUS_ICON="❌"
          STATUS_TEXT="Verification failed"
        fi

        {
          echo "markdown<<EOF"
          echo "## ${TITLE}"
          echo ""
          echo "- Status: ${STATUS_ICON} ${STATUS_TEXT}"
          echo "- SLSA posture: Level ${SLSA_LEVEL} via reusable workflow verification"
          echo "- Ref: \`${REF_NAME}\`"
          echo "- Image: \`${IMAGE}\`"
          echo "- Digest: \`${DIGEST}\`"
          echo "- Subject: \`${SUBJECT}\`"
          echo "- Signer workflow: \`${SIGNER_WORKFLOW}\`"
          echo ""
          echo "### Verify with GitHub CLI"
          echo ""
          echo '```bash'
          printf '%s\n' "${VERIFY_COMMAND}"
          echo '```'
          echo ""
          echo "> Authentication may be required. Run \`gh auth login\` or set \`GH_TOKEN\` before verification."
          echo ""
          echo "<details><summary>Verification details</summary>"
          echo ""
          echo '```text'

          if [ -n "${VERIFY_LOG}" ]; then
            printf '%s\n' "${VERIFY_LOG}"
          else
            echo "Verification succeeded. The GitHub CLI did not return additional output."
          fi

          echo '```'
          echo "</details>"
          echo "EOF"
        } >> "$GITHUB_OUTPUT"

    - name: Append step summary
      shell: bash
      env:
        MARKDOWN: ${{ steps.summary.outputs.markdown }}
      run: |
        set -euo pipefail
        printf '%s\n' "${MARKDOWN}" >> "$GITHUB_STEP_SUMMARY"
